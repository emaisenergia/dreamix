<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>Login - Dreamix</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"> <link rel="stylesheet" href="/style-auth.css">
    <style>
        /* Estilos adicionais para o botão "Ativar conta" */
        .activate-account-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            margin-top: 15px; /* Espaçamento do toast */
            padding: 12px 25px;
            background-color: #25D366; /* Cor do WhatsApp */
            color: white;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 600;
            font-size: 1rem;
            transition: background-color 0.2s;
            width: 100%; /* Ocupa a largura total do container */
            box-sizing: border-box;
        }
        .activate-account-btn:hover {
            background-color: #1DA851;
        }
        .activate-account-btn i {
            margin-right: 8px; /* Espaço entre ícone e texto */
            font-size: 1.1rem;
        }
        /* Ajustes para os toasts, se necessário para espaçamento */
        .toast {
            margin-bottom: 15px; /* Adiciona um pouco de espaço abaixo dos toasts */
            padding: 15px;
            border-radius: 8px;
            text-align: left;
        }
        .toast.error {
            background-color: #fef2f2;
            color: #ef4444;
            border: 1px solid #fecaca;
        }
        .toast.success {
            background-color: #ecfdf5;
            color: #10b981;
            border: 1px solid #a7f3d0;
        }
        /* Garantir que o botão 'Entrar' tem um bom espaçamento */
        .auth-form .btn {
            margin-top: 20px; /* Espaçamento acima do botão Entrar */
        }
    </style>
</head>
<body>
    <div class="auth-container">
        <a href="/"><img src="/assets/logo-completo.jpg" alt="Logotipo Dreamix" class="auth-logo"></a>
        <h2>Bem-vindo de volta!</h2>
        <p>Aceda à sua conta para continuar.</p>
        
        <% if (error) { %>
            <div class="toast error"><%= error %></div>
        <% } %>
        
        <% if (success) { %>
            <div class="toast success"><%= success %></div>
            <% if (whatsapp) { %>
                <a href="<%= whatsapp %>" target="_blank" rel="noopener noreferrer" class="activate-account-btn">
                    <i class="fab fa-whatsapp"></i> Ativar conta
                </a>
            <% } %>
        <% } %>

        <form action="/login" method="POST" class="auth-form">
            <label for="email">Email</label>
            <input type="email" id="email" name="email" required>
            <label for="password">Senha</label>
            <input type="password" id="password" name="password" required>
            <button type="submit" class="btn">Entrar</button>
        </form>
        <div class="auth-link">Não tem uma conta? <a href="/registo">Crie uma agora</a>.</div>
    </div>
</body>
</html>